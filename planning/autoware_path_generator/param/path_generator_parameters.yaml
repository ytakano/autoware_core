path_generator:
  planning_hz:
    type: double
    validation:
      gt<>: [0]

  path_length:
    backward:
      type: double
      validation:
        gt<>: [0]

    forward:
      type: double
      validation:
        gt<>: [0]

  waypoint:
    connection_gradient_from_centerline:
      type: double
      validation:
        gt<>: [0]

  turn_signal:
    search_time:
      type: double
      validation:
        gt_eq<>: [0]

    search_distance:
      type: double
      validation:
        gt<>: [0]

    resampling_interval:
      type: double
      validation:
        gt<>: [0]

    angle_threshold_deg:
      type: double
      validation:
        gt<>: [0]

  goal_connection:
    connection_section_length:
      type: double
      validation:
        gt<>: [0]

    pre_goal_offset:
      type: double
      validation:
        gt<>: [0]

  ego_nearest_dist_threshold:
    type: double
    validation:
      gt<>: [0]

  ego_nearest_yaw_threshold:
    type: double
    validation:
      gt<>: [0]
